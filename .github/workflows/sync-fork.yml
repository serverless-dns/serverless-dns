name: Sync Fork

on:
  schedule:
    - cron: '*/30 * * * *' # every 30 minutes
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.4
        with:
          token: ${{ secrets.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}}
          owner: llvm
          base: master
          head: master
