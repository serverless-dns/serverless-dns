{
  "private": true,
  "name": "serverless-dns",
  "version": "2.0.0",
  "license": "MPL-2.0",
  "description": "Rethink Free Dns with Blocklist, one click install from github to cloudflare",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf worker/ dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/serverless-dns/serverless-dns.git"
  },
  "author": "",
  "bugs": {
    "url": "https://github.com/serverless-dns/serverless-dns/issues"
  },
  "homepage": "https://github.com/serverless-dns/serverless-dns#readme",
  "dependencies": {
    "@serverless-dns/basic": "github:serverless-dns/basic#v2.2.1",
    "@serverless-dns/blocklist-wrapper": "github:serverless-dns/blocklist-wrapper#v2.2.1",
    "@serverless-dns/cache-wrapper": "github:serverless-dns/cache-wrapper#v2.1.4",
    "@serverless-dns/command-control": "github:serverless-dns/command-control#v2.2.1",
    "@serverless-dns/dns-operation": "github:serverless-dns/dns-blocker#v2.3.2",
    "@serverless-dns/dns-parser": "github:serverless-dns/dns-parser#v2.1.1",
    "@serverless-dns/lfu-cache": "github:serverless-dns/lfu-cache#v2.0.0",
    "dotenv": "^10.0.0",
    "node-fetch": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.7"
  }
}
